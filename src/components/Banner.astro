---
import Header from "./Header.astro";
const {heightBanner} = Astro.props
---

<section class=`hero-section flex flex-col bg-[url('../assets/bg-hero.png')] bg-cover bg-center bg-no-repeat h-[${heightBanner || '75vh'}] bg-black transition-all duration-300`>
    <Header />

    <div class="flex flex-col w-full h-full px-[20%] py-26">
        <slot />
    </div>
</section>